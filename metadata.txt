# This file contains metadata for your plugin.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=HA-Geophys Interpretation
qgisMinimumVersion=3.0
description=This plugin helps digitise greyscale pixel data into vector polygons. Supports PNG and TIFF input, with separate parameters for feature polygons and edge disturbance detection.
version=2.0
author=Lei Ding
email=lei.ding@headlandarchaeology.com

about=This plugin can help digitise pixels data to polygon and points.

tracker=http://bugs
repository=http://repo
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=no
# Uncomment the following line and add your changelog:
changelog=
    2.0
        - Removed point (ferrous object) creation as it was unreliable for greyscale-based detection
        - Removed geology overlap checkbox as it was dependent on point layer
        - Renamed "Point Parameter" to "Edge Disturbance Parameter" in UI for clarity
        - Edge disturbance polygon now uses its own dedicated pixel value range, separate from main polygon parameters
        - Added Simplify (Tolerance=0.1) and Smooth (Iterations=1, Offset=0.5) to all polygon outputs for cleaner edges
        - Default Minimum Area size changed to 5
    1.1 
        - add parameter for change the default map theme to other EPSG value to adapt to Irish project
    1.0
        - Initial release: pixel-to-polygon extraction from greyscale raster (PNG and TIFF)
# Tags are comma separated with spaces allowed
tags=python

homepage=http://homepage
category=Plugins
icon=icon.png
# experimental flag
experimental=True

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=

Category of the plugin: Raster, Vector, Database or Web
# category=

# If the plugin can run on QGIS Server.
server=False

